viewport
viewport可以理解为一个虚拟的窗口，每一款手机都有默认值，苹果手机的默认值一般是980px，如果设置为device-width那就是设备分辨率的宽度，viewport相当于把手机浏览器的宽平分成了980份，每份1px，如果在默认viewport下(980px)设置一个div宽为300px，那么在手机上看起来这个div占手机屏幕大概300/980的宽度，在网页中通常用meta标签对viewport进行设置，除了设置其宽度之外还可以设置初始放大系数，最小最大放大系数，是否可以自己放大缩小